#include <stdio.h>

/**
 * main -This is a programm that prints the string
 * with proper grammar, but the outcome is a piece of art
 * using the printf function and not puts
 * followed by a new line
 * Return: Alwat=ys 0 (Sucess)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
