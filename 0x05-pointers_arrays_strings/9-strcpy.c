#include <stdio.h>
#include <string.h>

/**
 * *_strcpy- copy string
 * @dest- copy to
 * @*scr- copy from
 *
 * Return: Pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	int i, temp, c;

	c = strlen(scr);
	for (i = 0; i < c; i++)
	{
		temp = scr[i];
		dest[i] = temp;
	}
	return (dest)
}
